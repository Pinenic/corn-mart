import { NotificationsClient } from "./NotificationsClient";

export const metadata = {
  title: "Notifications — Corn Mart",
};

export default function NotificationsPage() {
  return <NotificationsClient />;
}