import { CartClient } from "./CartClient";

export const metadata = {
  title: "Cart — Corn Mart Marketplace",
  description: "Review the items in your cart before checking out.",
};

export default function CartPage() {
  return <CartClient />;
}